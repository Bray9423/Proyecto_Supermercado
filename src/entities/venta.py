class Venta:

    def __init__(self, id, fecha, total, usuario_id, sucursal_id):
        self.id = id
        self.fecha = fecha
        self.total = total
        self.usuario_id = usuario_id
        self.sucursal_id = sucursal_id